const broken={a:1,b:2,c:3,d:4};function test(x:number){return x*2}const data=[1,2,3];

interface Broken{foo:string;bar:number;baz:boolean}

class BrokenClass{constructor(private val:number){}method(){return this.val*2}}